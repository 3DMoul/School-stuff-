#pragma once


#ifndef device_h
#define device_h
#include <string>

class Device
{
private:
	int id;
	std::string name;
public:	
	Device(int id, std::string name);// deklarerar konsturktor
	void Rename(std::string newname);// deklarerar function
	void Print();// deklarerar function
};
#endif
